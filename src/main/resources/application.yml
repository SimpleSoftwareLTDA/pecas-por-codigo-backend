spring:
  application:
    name: novo-pecas-online
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  mail:
    enabled: false
  jpa:
    open-in-view: false
  threads:
    virtual:
      enabled: true
app:
  url: ${CURRENT_URL:}
  site_url: ${SITE_URL:https://www.pecasonlinex.com.br}
