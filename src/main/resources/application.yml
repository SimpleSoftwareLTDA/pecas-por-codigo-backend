spring:
  application:
    name: novo-pecas-online
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    enabled: false
  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 10000
        order_inserts: true
        order_updates: true
  threads:
    virtual:
      enabled: true
  data:
    redis:
      host: backend.pecasonlinex.com.br
      port: 6379
      password: ${REDIS_PASSWORD:Backtalk1-Anyplace5-Magnolia8-Refute6-Coke7}
      time-to-live: 120000

app:
  url: ${CURRENT_URL:}
  site_url: ${SITE_URL:https://www.pecasonlinex.com.br}

decodo:
  proxy:
    host: br.decodo.com
    port: 10000
    username: ${DECODO_USER:spee0c0nv1}
    password: ${DECODO_PASS:b=9Bv5atx3GNWfqt4u}
